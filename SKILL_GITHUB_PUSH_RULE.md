# ⚔️ 银月钱庄 · Skill GitHub 推送铁律

> 本文件为银月钱庄项目**最高优先级规则**，所有 Agent/开发者必须遵守。
> 违反此规则 = 严重失职。

---

## 铁律第一条：Skill 必须上 GitHub 公开

**无论 Skill 是否成型、是否有 bug、是否文档齐全**，只要它是一个可辨识的 Skill（工具/插件/Agent/脚本），就必须推送到 GitHub 公开仓库。

| 场景 | 必须推送？ | 说明 |
|------|-----------|------|
| Skill 开发到一半 | ✅ 必须 | 半成品也是资产，分支保护即可 |
| Skill 有已知 bug | ✅ 必须 | 有 bug 才需要社区协作修复 |
| Skill 只有一个文件 | ✅ 必须 | 单文件也是知识沉淀 |
| Skill 文档未写 | ✅ 必须 | 文档可以后续补，代码先上 |
| Skill 只是概念验证 | ✅ 必须 | POC 也是宝贵经验 |

**例外（不推送）：**
- 包含硬编码密钥/Token 的文件
- 违反 GPL/AGPL 等传染性协议的第三方代码
- 客户保密协议约束的代码

---

## 铁律第二条：推送流程

```
开发/修改 Skill
    ↓
git add <skill files>
    ↓
git commit -m "skill: <skill 路径> - <改动摘要>"
    ↓
git push origin master
```

**不允许出现的情况：**
- ❌ "等写完了再推" → 直接违反铁律
- ❌ "这个不太成熟就不推了" → 直接违反铁律
- ❌ "忘记了" → 不可接受的借口
- ❌ "推私有仓库就行了" → 必须公开（除非有铁律第一条的例外）

---

## 铁律第三条：仓库可见性

默认仓库可见性：
- **所有 Skill 仓库 = 公开 (Public)**
- 除非有明确的合规/安全原因，否则不允许私有

---

## 铁律第四条：质量底线

虽然半成品也要推，但必须满足以下最低要求：
1. **无硬编码密钥** — Token/密码/API Key 必须走环境变量
2. **无恶意代码** — 不含后门、挖矿、数据窃取
3. **可识别用途** — 至少文件命名能看出它是做什么的

---

## 违规处罚

| 次数 | 后果 |
|------|------|
| 第 1 次 | 警告 + 补推 |
| 第 2 次 | 严重警告 + 写检讨书 |
| 第 3 次 | 降级处理（丧失自动推送权限） |

---

> **主人原话**："skill 成不成型，都必须要推上去 GitHub！"
> —— 2026-05-20，银月钱庄最高指令
