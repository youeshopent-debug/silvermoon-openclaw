# 银月刷屏修复 · 完成归档

**日期**: 2026-05-20  
**修复人**: 李长寿  
**问题严重度**: 高（日常对话完全不可用）

## 根因

双层问题叠加：
1. **数据源头** `lib/agent-tools.js:717` — `list_agents` handler 输出 `skills.slice(0,3).join('、')`，每个 skill 是 TASK.json 中的完整描述字符串（含冒号后详细说明），如 "crawlee: 电商平台爬虫（AliExpress/Amazon/1688 选品数据...）"
2. **输出通道** `main.js` 中 4 处 handler（TG×2 / Discord / Agent Bot）— 模板 `🔧 工具[工具名]: {result}` 直接输出原始返回，无截断

## 修复方案（双层保险）

| 层级 | 文件 | 改动 |
|------|------|------|
| 源头截断 | `lib/agent-tools.js` | skill 只取冒号前短名称，最长 25 字符 |
| 通道限流 | `main.js`（4处handler） | 工具结果超 500 字符自动截断并加 `…（结果过长已截断）` |

## 多场景验证结果（全部通过）

| 场景 | 回复 | 长度 | 结论 |
|------|------|------|------|
| "空挡的agent有谁" | 仅列空闲Agent | 147字符 | ✅ |
| "agent清单" | 分组列出，无技能详情 | 294字符 | ✅ |
| "今天天气怎么样" | 正常反问城市 | 26字符 | ✅ |
| "系统运行正常吗" | 格式化状态报告 | 141字符 | ✅ |

## 变更文件

- `C:\Users\User\.openclaw\lib\agent-tools.js` — skills 截断
- `C:\Users\User\.openclaw\main.js` — 4处 handler 加截断

**状态**: ✅ 全部就绪，生产可用
