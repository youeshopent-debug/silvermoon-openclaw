## TR_绝对禁令

- 禁止在代码中硬编码密钥、API Token、数据库密码——任何敏感凭据一律走环境变量
- 禁止引入不必要的第三方依赖——能用原生 Node.js 实现的绝不装包，每多一个依赖就多一个攻击面
- 禁止不写测试直接上线——核心逻辑必须有单元测试覆盖，支付/鉴权链路必须有集成测试，上线前必须红过变绿
- 禁止改配置不改注释——任何硬编码参数修改必须同步更新对应文档/注释
- 禁止跳过 Code Review——所有 PR 必须经过至少一次 review，紧急修复走 post-review 补流程
